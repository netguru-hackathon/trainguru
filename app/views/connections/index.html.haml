%h1 Listing connections

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @connections.each do |connection|
      %tr
        %td= link_to 'Show', connection
        %td= link_to 'Edit', edit_event_connection_path(connection)
        %td= link_to 'Destroy', connection, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Connection', new_event_connection_path
